@{
    ViewData["Title"] = "Carta Misteriosa";
}

<div class="habitacion3-container">
    <div class="habitacion3-content carta-content">
        <h1 class="titulo-habitacion">Carta Misteriosa</h1>

        <div class="carta-texto">
            <div class="descripcion">
                <p>Al tomar la carta del escritorio, descubres que contiene una serie de enigmas escritos con una caligrafía antigua...</p>
            </div>

            <div class="acertijos">
                <p>Para avanzar, deberás resolver estos enigmas:</p>
                <ol>
                    <li>Soy guardián de historias, pero no puedo leer.</li>
                    <li>Mis páginas están en blanco, pero contengo todo el saber.</li>
                    <li>En la oscuridad brillo, en la luz me desvanezco.</li>
                </ol>
            </div>

            <form asp-controller="Home" asp-action="VerificarHabitacion3" method="post" class="form-respuesta">
                <div class="input-container" data-tooltip="Ingresa la respuesta al enigma">
                    <input type="text" 
                           required
                           name="respuesta"
                           placeholder="Escribe tu respuesta aquí..."
                           autocomplete="off" />
                </div>
                <button type="submit">Revelar el Secreto</button>
            </form>

            @if (TempData["Error"] != null)
            {
                <div class="error">
                    @TempData["Error"]
                </div>
            }
        </div>

        <a href="@Url.Action("Habitacion3", "Home")" class="btn-volver">Volver a la Habitación</a>
    </div>
</div> 